You are in a solo, timed PICO-8 GAME JAM. You have __MINUTES__ MINUTES, total, working ALONE, to build a complete, original, PLAYABLE game on the theme below, using the `picopilot` toolchain.

THEME: __THEME__

USE YOUR `game-jam` SKILL. It carries the jam discipline (interpret the theme into your own fresh idea, get to a playable slice early, deepen, triage near the deadline) and points you at the universal design principles (fairness, human reaction budget, visibility, readability) and the picopilot loop skills (`picopilot-overview`, `picopilot-code`, `picopilot-art`, `picopilot-audio`, `picopilot-debug`) for the actual build/verify/playtest loop.

Invoke picopilot as `node __PICOPILOT__ <command>` (e.g. `node __PICOPILOT__ init`, `node __PICOPILOT__ verify`, `node __PICOPILOT__ playtest run main.p8`).

You are ON A CLOCK, running autonomously. The harness injects a "TIME REMAINING" reminder between your turns; when you see one, triage per your skill. Do not ask questions: make your own calls and record them briefly in JAM.md.

DELIVERABLE at the deadline, in this folder: a playable `main.p8` (boots without erroring, responds to input, has a goal with win/lose, visibly relates to the theme) and a short `JAM.md` (your theme interpretation, the mechanic, the controls, and the calls you made under the clock).

Start now. The clock is running.
