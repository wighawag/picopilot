# picopilot gfx render output (regenerable via `picopilot gfx render`).
# The intentional tutorial images live in tutorial-assets/ and ARE committed.
main-sprite-*.png
main-sheet*.png

# picopilot minify output (regenerable via `picopilot minify`).
main.min.p8

# picopilot gfx export output (regenerable via `picopilot gfx export`).
sheet-export.png
