/**
 * The static content `picopilot init` scaffolds: the `#include`-discipline
 * `main.p8` (built through the cart model), the editable `main.lua`, the curated
 * `AGENTS.md` PICO-8 reference, and the `picopilot.json` config. See
 * `scaffold.ts` for the single scaffolding model and why these are the files.
 */
export {
	agentsMd,
	LUA_INCLUDE_LINE,
	mainLua,
	mainP8,
	picopilotJson,
	type ScaffoldFile,
	scaffoldFiles,
} from './scaffold.js';
