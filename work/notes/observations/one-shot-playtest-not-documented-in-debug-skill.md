# one-shot `playtest` never landed a section in the picopilot-debug skill

2026-07-06: while building `playtest-resumable-session` I noticed the landed one-shot task (`playtest-one-shot-drive-capture`) shipped the `picopilot playtest` command + its `engine/pico8/drive.ts` transform, but did NOT add a `playtest` section to `src/skills/picopilot-debug/SKILL.md` (the skill still only documents `verify`, `run`, and `run --input`). The resumable-session task's own acceptance ("the picopilot-debug skill documents both the live session (A) and the free stateless-replay (C) patterns") led me to add a `playtest` section covering the one-shot, the live session, AND the replay pattern in one place, so the gap is now filled. Flagging in case the one-shot task's acceptance intended a skill section that was missed.
