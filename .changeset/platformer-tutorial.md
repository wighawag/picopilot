---
"picopilot": patch
---

Add a hands-on platformer tutorial (`examples/platformer/`) that demonstrates driving a coding agent to build a PICO-8 game with picopilot v1-core: `init` scaffolding, the `tokens` budget loop, the `gfx set` → `gfx render` → look-and-fix "eyes" loop (with three real sprite iterations), and the static `verify` gate. Written as a real build with human-in-the-loop PICO-8 run checkpoints; includes a complete, playable cart (walk / gravity / jump / climbable staircase, 356 tokens).
